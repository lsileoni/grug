# Normalize line endings: LF in the repo for all text, regardless of platform.
* text=auto eol=lf

# PowerShell scripts are happiest with CRLF on Windows.
*.ps1 text eol=crlf

# Never touch binaries.
*.exe binary
*.o   binary
*.obj binary
